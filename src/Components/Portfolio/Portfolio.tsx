import React, {FC} from "react";
import './Portfolio.scss';

const Portfolio: FC = () => {
    return (
        <div className="portfolio">
            Portfolio
        </div>
    );

};

export default Portfolio;